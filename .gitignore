# ============================================
# .gitignore —— Git 的"黑名单"
# 写在这里的文件/目录，Git 一律视而不见，永远不会被提交
# ============================================

# --- 环境变量与密钥（最重要的一条）---
# .env 用来存数据库密码、API 密钥等本地机密配置
# 第 23 天才会写入真实内容，但规则必须现在就位
# 原因：Git 记录的是全部历史，一旦上传过一次，删掉也还在历史里
.env
.env.*
!.env.example

# 说明：.env.* 用通配符一次挡住 .env.production / .env.development / .env.test
# 等所有"带环境后缀"的名字（逐个枚举一定会漏掉以后新增的）；
# 上一行的 ! 是"例外语法"，把 .env.example（模板文件，本来就该提交给同学看）
# 从黑名单里捞回来。

# --- 证书与私钥（拿到就等于拿到钥匙）---
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519

# --- 数据库文件与导出（含真实数据，体积也大）---
*.db
*.sqlite
*.sqlite3
*.sql
*.dump

# --- 云服务凭据与包管理器的私有配置 ---
credentials.json
service-account*.json
.npmrc
.yarnrc

# --- 测试覆盖率报告（本地生成，随时可重跑）---
coverage/

# --- AI 协作工具的本地工作目录 ---
# .workbuddy/ 装的是本机的协作笔记与记忆，属于"个人台面"
# 它就在项目文件夹里，和 .env 一样靠这一行挡住，别让它进公开仓库
.workbuddy/

# --- 依赖包目录（体积巨大，且可由 package.json 还原）---
node_modules/
vendor/

# --- 编辑器与操作系统生成的杂项文件 ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# --- 日志文件（本地调试产物，无留存价值）---
*.log
npm-debug.log*
yarn-error.log*

# --- 构建产物与缓存 ---
dist/
build/
.cache/
