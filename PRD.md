# PRD —— 英语学习网页工具

> **项目**：英语学习网页工具（面向本机构学生的内部学习工具）
> **仓库**：https://github.com/XingHo-VibeCoding/mirroriceee
> **文档阶段**：Day 4 · 第 1 周｜产品需求文档（PRD）
> **日期**：2026-09-19
> **依据**：`research.md`（Day 3 需求研究）
> **本文档给谁看**：自己（动手时的依据）、老师与同学（验收时的对照物）、AI（写代码时的输入）

---

## 目录

- [〇、一句话说明](#〇一句话说明)
- [一、背景与问题](#一背景与问题)
- [二、目标用户与核心场景](#二目标用户与核心场景)
- [三、产品定位与形态](#三产品定位与形态)
- [四、功能范围（本期要做）](#四功能范围本期要做)
- [五、本期不做（及什么条件下翻案）](#五本期不做及什么条件下翻案)
- [六、验收标准](#六验收标准) ← **验收时只看这一节**
- [七、里程碑与风险](#七里程碑与风险)
- [附录：决策台账](#附录决策台账)
- [修订记录](#修订记录)

---

## 〇、一句话说明

**把老师课上要求背的词、写的文、读的课文，收拢到一个学生打开就能用的网页里，不用在微信群和多个 App 之间来回翻。**

本期先做其中一件：**文本跟读（复读机）**。理由见 4.3。

---

## 一、背景与问题

### 1.1 现状（我们自己的真实体验）

- 背单词要单独开一个 App，词库和课上要求对不上，还得忍受广告。
- 课文跟读的音频/视频散在群聊里，想重复听某一段要手动拉进度条、来回找。
- 作文模板要翻群聊记录。
- 老师收作业也在微信群，视频一条条发，谁交了谁没交全靠往上翻。

### 1.2 我们要解决的问题

学生不是缺学习工具，是缺**一个把课上内容集中起来、打开就能用的地方**。现有一堆工具各自的毛病是：内容不在课程范围内、广告多、手机上难用。

### 1.3 一个关键判断：竞品不是同类产品

**我们要替代的是「微信群 + 现有的笨办法」，不是去打败百词斩或多邻国。**

学生不用我们的产品，不是因为百词斩更好，而是因为"发群里"这个笨办法**还能凑合**。要赢的就是那个"还能凑合"。

推论：产品的价值不在"功能多"，在**内容边界清晰（只含本课程内容）+ 打开就用**。

---

## 二、目标用户与核心场景

### 2.1 目标用户

- **主体**：本机构的学生（以大学生为主），由老师发放验证码进入。
- **非目标用户**：社会上的泛英语学习者（不对外流通，也不打算争取）。

### 2.2 核心场景（按重要性排序）

| # | 场景 | 用户的期待 |
|---|---|---|
| S1 | 晚上在宿舍，掏出手机想练今天课文里某几句 | 打开就能听，**想听哪一句点哪一句**，不用拉进度条找 |
| S2 | 课前想先过一遍今天要背的词 | 词表就是课上那批，不多不少 |
| S3 | 写作文前想找个模板参考 | 模板在课程范围内，不用翻群聊记录 |

### 2.3 设备前提（由研究证据推出的硬约束）

学生大概率是**掏出手机**就用。研究里批改网被骂得最狠的一条是「卡顿 + 手机端页面不适配」，而这条**躲不掉**——它跟商业模式无关，纯粹是工程质量问题。

> **因此：移动端适配是刚需，不是加分项。**

---

## 三、产品定位与形态

### 3.1 定位

**面向特定机构的内部英语学习工具**：不对外流通、不含广告、内容不超出课程范围。

### 3.2 是什么 / 不是什么

| 是 | 不是 |
|---|---|
| 一个打开就能用的网页 | 一个内容平台，不追求流量 |
| 课程内容的集中入口 | 通用英语学习 App 的替代品 |
| 老师发码、学生凭码进入的内部工具 | 公开注册、对外推广的产品 |

### 3.3 产品形态

**纯前端静态网页**（无服务器、无数据库、学生不上传任何文件），响应式，手机优先。

### 3.4 进入方式：验证码（本期已定）

| 项 | 结论 |
|---|---|
| 粒度 | **每个班级一个验证码** |
| 有效期 | **长期有效**，跨学期沿用 |
| 码的作用 | 入场凭证 + **班级归属依据**（输哪个班的码，就进哪个班的内容） |
| 学期结束怎么处理 | **更换验证码**，旧码立即失效 → 该班全体失效 |
| 学生毕业 | 不再需要单独处理（该班码已在流程中被更换） |
| 是否需要知道"谁是谁" | **不需要**（本期不按人管理授权） |

**为什么本期不做"按人管理授权"**：那需要一份学生名单，并且要能远程修改它——纯前端的代码跑在学生浏览器里，做不到。详见第五节与附录决策台账。

#### 已知限制（如实记录，不假装没有）

纯前端意味着**校验逻辑在用户手里**：它挡得住随手点进来的人，**挡不住按 F12 的人**。本期接受这个限制，理由是我们面对的是本班同学，而不是公开互联网。

---

## 四、功能范围（本期要做）

### 4.1 范围总览

| 编号 | 功能 | 优先级 | Day 7 MVP |
|---|---|---|---|
| F1 | 验证码进入 | P0 | ✅ 必须 |
| F2 | 文本跟读（复读机） | P0 | ✅ 必须 |
| F3 | 首页骨架（导航到各板块） | P0 | ✅ 必须（简版） |
| F4 | 词汇速记 | P2 | ❌ MVP 后置 |
| F5 | 作文模板 | P2 | ❌ MVP 后置 |

---

### 4.2 F1 验证码进入

**需求**

| 编号 | 需求 |
|---|---|
| F1-1 | 打开网址，先看到入口页：一个输入框 + 一个"进入"按钮。除入口页外，任何内容页面都不允许在未验证的情况下打开。 |
| F1-2 | 输入正确的班级验证码 → 进入该班的跟读页，页面上要能看出当前是哪个班。 |
| F1-3 | 输入错误的验证码 → 停留在入口页，给出提示，**不跳转、不显示任何素材内容**。 |
| F1-4 | 验证码可更换；更换后旧码立即失效（用于学期末换码）。 |
| F1-5 | 验证码**不得以明文形式出现在公开仓库中**（怎么做到，留到 Day 5 技术选型确认）。**降级约定**：若纯前端在技术上做不到（校验逻辑必须发到浏览器），本条降级为"已知限制"，按 3.4 如实记录实际做法，**不视为 MVP 未达标**。 |

**已知限制**：纯前端校验可被绕过（见 3.4）。本期不为此增加复杂度。

---

### 4.3 F2 文本跟读（复读机）★ MVP 核心

**为什么先做它**：三个板块里，**"为什么必须是网页"最站得住的就是它**。微信群能发视频，但不能当复读机用——想反复听某一句，只能手动拉进度条。这正是这个产品不能被"发群里"替代的唯一一块。

**需求**

| 编号 | 需求 |
|---|---|
| F2-1 | 页面顶部是素材列表（本期先用 1–2 条）。选择一条后，下方显示对应的课文文本。 |
| F2-2 | **文本按句切分，点某一句即播放该句对应的音频片段**（"分段点播"）。 |
| F2-3 | 提供**单句重复**：打开重复开关后，当前句播完自动重播，直到关闭。 |
| F2-4 | 播放控制：播放 / 暂停 / 停止；**当前正在播放的那一句要有可见高亮**，并随播放进度移动。 |
| F2-5 | 播放中点击另一句 → 立即切到新句（不允许两段声音同时响）。 |
| F2-6 | 播放器要能在手机上单手操作：**播放/重复按钮的点击区域不小于 44×44 像素**。 |
| F2-7 | **本期素材限定为纯音频文件**（依据：作者已确认可拿到全部教学音频；视频不作为本期素材）。开发期先用公共领域音频跑通功能，教学音频到位后替换，不阻塞开发。**若后续要引入视频，必须重评 R8 的体积阈值与分段方案。** |

**已识别的两笔账（写进风险，见 7.2）**

1. **素材是内容工作量**，不是技术工作量。
2. **媒体文件拖慢加载** —— 控不好，就会原封不动地重演批改网那条"卡顿"差评。

**留到 Day 5 的问题**：句子与音频的"对齐数据"（哪一句从第几秒到第几秒）怎么来——手工标注还是工具生成？这直接决定 F2-2 的可行性与工作量。

---

### 4.4 F3 首页骨架（简版）

**需求**

| 编号 | 需求 |
|---|---|
| F3-1 | 验证通过后先到首页：顶部显示**当前班级标识**，下方列出三个板块入口（文本跟读 / 词汇速记 / 作文模板） |
| F3-2 | 每个入口都要**标明当前状态**（可用 / 开发中），不允许出现"点进去才发现是空的" |
| F3-3 | 任何内页都能一键回到首页，不出现"走不回去" |
| F3-4 | 本阶段首页**只求可用、不求好看**——"吸睛主界面"属后期（见第五节第 6 行） |

---

### 4.5 F4 词汇速记（MVP 后置）

课程范围内的固定词表：浏览 + 标记"已掌握"。不做发音评测，不做自定义词库。

### 4.6 F5 作文模板（MVP 后置）

课程范围内的模板列表 + 单篇查看。**不做**提交、不做批改（见第五节）。

---

### 4.7 Day 7 MVP 的交付边界（本期已定）

**MVP = F1 验证码进入 + F2 复读机 + F3 首页骨架。**

词汇速记与作文模板**不是砍掉，是后置**——它们仍在本期范围内，只是排在 MVP 之后。理由：先把一条完整链路跑通、能拿给老师和同学看，比三个板块各做一半强。

---

## 五、本期不做（及什么条件下翻案）

「不做」不是判死刑，是排队。每一刀都记下理由和翻案条件。

| # | 本期不做 | 理由 | 什么条件下翻案 |
|---|---|---|---|
| 1 | **AI 批改 / 接入知识库** | 需要 LLM API（费用 + 后端）；且会形成"AI 给建议、老师还得重看"的双倍工作量；不是我们的护城河 | 复读机被人真的用起来之后，且批改确实是瓶颈 |
| 2 | **学生上传录音 / 视频** | 一旦接收文件，就要存储、上传链路、鉴权，后端随之而来 | 上后端之后（见第 4 行） |
| 3 | **老师端与作业批改** | 网页和微信群在"收作业"这件事上差别不大——老师直接在群里点开视频看就行；自建提交与批改工程负担很重。**作业仍然走微信群** | 老师明确提出"群里翻视频太累" |
| 4 | **后端 / 服务器 / 数据库，及依赖它的「每人一码、按人审批、后台撤销授权」** | 砍掉上传后，产品成为纯前端静态网页，不需要服务器和数据库；而"按人管理授权"需要一份**能远程修改的学生名单**——纯前端存不了也改不了，硬编码进代码还会把名单送上公开仓库（隐私风险） | **真有人持续使用，且确实需要按人（而不是按班）管理授权**（见附录决策台账） |
| 5 | **小程序 / App** | 本期先把网页做扎实。另据查证，个人主体小程序不支持内嵌网页，迁移等于前端重写 | 网页被验证有效，且机构愿意出企业主体 |
| 6 | **吸睛主界面** | 主界面是最后打磨的东西。功能先跑通，再谈好看 | MVP 的验收标准全部通过之后 |
| 7 | **广告 / 任何形式的商业化** | 不对外流通，靠老师发码分发，天然不需要变现 | 不需要翻案：这是产品定位的一部分 |

---

## 六、验收标准

> **这一节是"怎么算做完了"的唯一依据。**
> 写法遵循一个公式：**在什么条件下 → 做什么操作 → 应该看到什么结果**。
> 不写形容词（"友好""流畅"无法判定），不省略坏路径（只写正常情况等于没写）。
> 小节顺序按**用户动线**排：验证码 → 首页 → 复读机（与第四节的 F 编号顺序不同，不是笔误）。

### 6.1 全局标准（适用于所有页面）

| 编号 | 标准 | 怎么测 |
|---|---|---|
| G1 | 在 **375px 与 390px 宽**的屏幕上打开任一页面，**无需横向滚动**，主要按钮完整可见、可点 | 用手机真机打开，不是只在电脑上把窗口缩窄 |
| G2 | 手机 4G 网络下打开跟读页，**3 秒内**可点击播放 ⚠️ 此阈值为**假素材**阶段的暂定值，真素材到位后必须重测并更新 | 手机真机 + 秒表 / 浏览器开发者工具 |
| G3 | 页面上**不出现任何广告位、弹窗广告、跳转第三方的营销链接** | 通读页面，逐个点开可疑元素 |
| G4 | 公开仓库与页面源码中**不出现明文的验证码、学生姓名、老师联系方式** | 在仓库和页面源码里搜关键字 |
| G5 | Chrome / Edge 桌面端，以及手机 Chrome / iOS Safari 上，核心功能可正常使用；**iOS Safari 上音频能真实出声**（注意移动端浏览器对自动播放的限制） | 用真机各测一遍，不以电脑上的"缩窗口"代替 |
| G6 | 页面里的中文不出乱码，字体大小在手机上看不需要放大 | 手机真机目视 |

### 6.2 F1 验证码进入

| 编号 | 标准 |
|---|---|
| V1 | 输入**正确**的班级验证码 → 进入跟读页，页面上能看出当前是哪个班 |
| V2 | 输入**错误**的验证码 → 停留在入口页并出现提示，**页面不出现任何素材内容** |
| V3 | **不输入**直接点"进入" → 提示"请输入验证码"，不发生跳转 |
| V4 | 验证码**前后带空格**输入 → 视为正确（自动去空格） |
| V5 | **更换**验证码后：旧码输入被拒；新码可正常进入 |
| V6 | 未验证时，通过任何地址进入（含带 # 的地址）或直接刷新页面 → 都停在入口页，**看不到任何素材内容** |

### 6.3 F3 首页骨架

| 编号 | 标准 |
|---|---|
| H1 | 验证通过后到首页 → 页面上能看到**当前班级标识**，以及三个板块入口（文本跟读 / 词汇速记 / 作文模板） |
| H2 | 每个入口都**标明了状态**；点击"开发中"的入口 → 出现明确提示，**不出现空白页、不出现死链** |
| H3 | 从任意内页点"返回首页" → 回到首页；三个入口都能进去再回来，不出现"走不回去" |

### 6.4 F2 文本跟读（复读机）

| 编号 | 标准 |
|---|---|
| R1 | 点击任意一句文本 → **1 秒内**听到该句音频，且**只有该句在播** |
| R2 | 同一句**连续点击 3 次** → 每次都从头播该句，**不出现两段声音重叠、不出现卡住不出声** |
| R3 | 播放中点击**另一句** → 当前播放立即停止，改播新句，不叠加 |
| R4 | 当前正在播放的句子有**可见高亮**，切换句子时高亮跟随 |
| R5 | 打开**单句重复**开关 → 该句播完自动重播；关闭后播完即停 |
| R6 | 素材**加载中**有加载状态提示；**加载失败**（如断网）显示可读的错误提示 + 重试入口，**不出现白屏** |
| R7 | 素材列表里切换第二条素材 → 第一条立即停止播放（不出现两条素材同时响） |
| R8 | **单条音频素材**体积不超过 5MB（暂定值，真素材到位后按实际音质需求复测调整；**本条只适用音频，不适用视频**） |
| R9 | 播放/重复按钮的**可点击区域不小于 44×44 像素**，手指点得准 |

### 6.5 坏路径（必须逐条测，这些是差评的出生地）

| 编号 | 标准 |
|---|---|
| B1 | **断网**状态下打开页面 → 出现"加载失败 / 请检查网络"提示，不是白屏、不是一直转圈 |
| B2 | 某条素材文件缺失（404）→ 该条目显示"素材暂不可用"，**其余条目仍能正常使用** |
| B3 | 页面切到后台再切回来 → 播放状态不崩溃；音频位置可恢复或干净地停下 |
| B4 | 播放过程中点击"停止" → 音频立刻停止，高亮回到未播放状态 |

### 6.6 谁来验收、怎么算通过

| 项 | 约定 |
|---|---|
| **验收人** | 第一轮：项目作者本人（斌哥）；第二轮：至少 1 位同学 + 老师（要第二意见） |
| **验收时间** | Day 7 的 MVP 产出后**当天**即验，不拖 |
| **验收方式** | 对照本节的表格逐条操作；涉及时间的用秒表/开发者工具；**移动端相关的一律用真机**，不许用"电脑上缩窗口"代替 |
| **记录方式** | 每条打勾；不合格的写明"实际看到什么"，不写"感觉不行" |
| **判定** | 全部 G / V / H / R / B 条目通过 = MVP 达标；**有任何一条不合格 = 未达标**，逐条修完再复验 |

---

## 七、里程碑与风险

### 7.1 里程碑

| 时间 | 交付 |
|---|---|
| Day 4（今天） | 本文档 `PRD.md` 定稿并入库 |
| Day 5 | 技术选型（在本文档范围内选，不扩大范围）；同时评估 4.3 里"句子与音频对齐数据"的可行做法 |
| Day 6 | 按课程安排推进（本 PRD 不预设该日交付物）。若该日留空，则用于把 F1 验证码 + F3 首页做成可跑版本 |
| Day 7 | **MVP**：F1 + F2 + F3，并跑完第六节的全部验收标准 |
| MVP 之后 | F4 词汇速记 → F5 作文模板 → 主界面打磨 |

### 7.2 风险与对策

| # | 风险 | 影响 | 对策 |
|---|---|---|---|
| 1 | **媒体文件拖慢加载** | 直接重演批改网"卡顿"的差评 | 控制单条素材体积（R8）、按需加载、真素材到位后按 G2 重测 |
| 2 | **教学音频的到位时间**（作者已确认能拿到全部教学音频） | 若迟迟不到，复读机就是空壳 | 假素材先行开发；Day 7 前至少换入 1 条真实音频验证 |
| 3 | **句子与音频的对齐数据是隐藏工作量** | 可能比写播放器还费时间 | Day 5 专门评估：手工标注 vs 工具生成 |
| 4 | **范围膨胀**（研究阶段曾一路加功能：老师端 → 学生互见 → AI 批改 → 后台） | MVP 做不完 | 第五节「本期不做」清单 + 每一条都写明翻案条件 |
| 5 | **纯前端的门禁挡不住有心人** | 内容可能外流 | 已知限制，本期接受（3.4）；真需要硬门禁时翻案第 4 行 |
| 6 | 移动端音频在 iOS Safari 上的播放限制 | iPhone 用户听不了 | 列入验收标准 G5，Day 5 选型时确认实现方式 |
| 7 | **验收人本身是对外依赖**（6.6 要求同学 + 老师给第二意见） | 约不到人 → MVP 做完了却没人确认，"完成"悬着 | 提前跟老师、同学约好时间；真约不到，先自己按 6.6 逐条自验并留存记录（截图/录屏），**不把"没人看过的验收"当成通过** |

### 7.3 已知的账（诚实记录，不假装不存在）

1. **素材体积与加载速度**：暂定阈值 5MB / 3 秒，都是假素材阶段的估值，真素材到位后必须重测。
2. **门禁强度**：本期是"防路人"级别，不是"防有心人"级别。
3. **词汇速记与作文模板**：在本期范围内但排在 MVP 之后，Day 7 交付时**不包含**。

---

## 附录：决策台账

> 记下每个决定是谁拍的、基于什么理由——将来想翻案时，先回头看这里。

### Day 3（需求研究）拍下的取舍

| 决定 | 理由 |
|---|---|
| 砍掉「学生提交 + 老师批改」整个模块 | 网页与微信群在收作业上差别不大，而自建提交与批改工程负担很重 |
| 砍掉 AI 批改（作文不引入 AI） | 需 LLM API + 后端；且会形成"AI 给建议、老师还得重看"的双倍工作量 |
| 砍掉学生上传录音 / 视频 | 需要存储与鉴权，后端随之而来 |
| 砍掉吸睛主界面（移后期） | 主界面是最后打磨的东西 |
| 小程序 / App 列入"以后" | 个人主体小程序不支持内嵌网页，迁移等于前端重写 |
| **先做文本跟读** | 三个板块里，"为什么必须是网页"最站得住的就是它 |
| 验证码机制（三轮迭代后定稿） | 每班一个码、长期有效、学期末换码 = 整班失效 |

### Day 4（本文档）拍下的四个板

| # | 决定 | 理由 | 翻案条件 |
|---|---|---|---|
| 1 | **本期不做后台**（A 档：纯前端 + 每班一个长期码） | ① 现在不确定的是"有没有人用"，不是"门禁够不够硬"② 学期末换码已达成"学期结束移出"的目的，代价只是一行配置 ③ 上后台会让 Day 7 的 MVP 有做不完的风险 | 真有人持续使用，且确实需要**按人**管理授权 |
| 2 | **Day 7 的 MVP 只保复读机**（+ 首页 + 验证码） | 先把一条完整链路跑通、能拿给老师同学看，比三个板块各做一半强 | MVP 验收通过后即可推进 F4 / F5 |
| 3 | **复读机做「分段点播」** | 跟读真正卡人的不是"整段没听过"，是"某一句听不清楚"；只有分段点播才对得上 S1 场景，也才是"必须用网页"的理由 | 视使用反馈决定是否加 A-B 循环与倍速 |
| 4 | **素材先用假素材开发** | 素材是内容工作量，不该卡住技术工作 | 教学音频到位后替换并重测 G2 / R8 |

---

**文档状态**：Day 4 定稿，随 Day 4 的提交入库。后续如与 `research.md` 冲突，以本文档为准（本文档更晚、更具体）。

---

## 修订记录

| 版本 | 日期 | 阶段 | 改了什么 |
|---|---|---|---|
| v1.0 | 2026-09-19 | Day 4 | 首版落盘：七节结构 + 28 条验收标准。含自检修正 5 处：① 补 F3「首页骨架」需求条目 ② F1-5 增加降级约定 ③ 素材格式锁定为音频（原"音视频"含糊，5MB 阈值对视频不成立）④ V6 由"访问内容页地址"改为可执行的表述 ⑤ 修正"已入库"的不实措辞（当时尚未提交） |
